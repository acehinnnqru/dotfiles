return {
    -- enable mouse
    mouse = "a",

    -- integrated with clipboard
    -- unix
    clipboard = "unnamedplus",

    -- search options
    hlsearch = true,
    ignorecase = true,
    smartcase = true,

    -- split view options
    splitbelow = true,
    splitright = true,

    -- tabline options
    showtabline = 1,

    -- pop menu, aka completion menu
    pumheight = 10,
    completeopt = { "menu", "menuone" },

    -- file options
    fileencoding = "utf-8",

    -- enable undofile
    undofile = true,

    -- editor behaviors
    joinspaces = false,
    writebackup = false,

    -- gutter options
    number = true,
    relativenumber = true,
}
